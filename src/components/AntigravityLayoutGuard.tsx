'use client';

/**
 * AntigravityLayoutGuard — Hides legacy layout chrome (header/footer/nav).
 * Antigravity is the only UI; each page renders its own navbar/footer.
 */

export function AntigravityLayoutGuard({ children }: { children: React.ReactNode }) {
  return null;
}
